# Base URL for the backend API.
# Same-origin web / PWA (served behind the same Caddy as /api): leave as /api.
# Capacitor / cross-origin builds: set an absolute HTTPS URL, e.g.
# VITE_API_BASE_URL=https://89-111-137-65.swtest.ru/api
VITE_API_BASE_URL=/api
